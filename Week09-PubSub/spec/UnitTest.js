describe( "Module Test", function(){
	
it( "expects Calculate.add (3) to be 7", function(){
expect(Calculate.add(3, 4)).toBe(7);
} );
it( "expects Calculate.multiply (3,4) to be 12", function(){
expect(Calculate.mutiply(3, 4)).toBe(12);
} );
it( "expects Calculate.subtract (7, 3) to be 4", function(){
expect(Calculate.subtract(7, 3)).toBe(4)
} );