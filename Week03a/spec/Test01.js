describe("numberGetter Test Suite", function() {
	
  it("contains spec with an expectation for getNine()", function() {
    expect(numberGetter.getNine()).toBe(9);
  });
  
  it("contains spec with an expectation for getEight()", function() {
    expect(numberGetter.getEight()).toBe(8);
  });
});